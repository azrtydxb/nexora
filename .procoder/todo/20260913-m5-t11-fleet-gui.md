# M5 Task 11: Fleet GUI

Status: closed 2026-09-14
Created: 2026-09-13

## Description

Implement Task 11 ("Fleet GUI") of milestone M5 exactly as specified in
`.procoder/plans/nexora-v1-m5.md`, following `docs/architecture.md`. Done means
the task's tests exist, fail before the change, pass after it, and the change
is committed.

## Acceptance criteria

- [x] Every step of Task 11 in `.procoder/plans/nexora-v1-m5.md` is done as written (deviations recorded in the plan first)
- [x] `TestGUICoverage` passes in the dev pod (`scripts/dev-exec.sh`)
- [x] `TestGUIFleet` passes in the dev pod (`scripts/dev-exec.sh`) — no such test exists; the fleet GUI specs 20/21 run inside `TestGUICoverage` (N/A names: TestGUIFleet)
- [x] procoder gate clean over the changed files; work committed

## Evidence

- Red: `scripts/dev-exec.sh 'make e2e-build && NEXORA_E2E_BIN_DIR=$PWD/bin go test ./e2e/ -run TestGUICoverage -count=1'` with the glob widened and specs 20/21 present -> FAIL, 22 passed, 2 failed: `getByTestId('fleet-summary')` element(s) not found (20) and `rewrite-engine-group` timeout (21).
- `scripts/dev-exec.sh 'make web-test'` -> typecheck, eslint, permission parity (110 operations) and vite build clean.
- `make e2e-build` -> web-build and cargo build ok, then `cp: cannot create regular file 'bin/nexora-engine': Text file busy` (the concurrent fleet agent's tests hold the binary); the remaining steps were run by hand (engine copied via rename, `go build` of nexora-mgmt and nexora-fixture).
- `NEXORA_E2E_BIN_DIR=$PWD/bin go test ./e2e/ -run TestGUICoverage -count=1` -> `ok github.com/piwi3910/nexora/e2e 91.233s` (all 24 specs including 05-engines, 20-fleet, 21-engine-group-scope pass; every OpenAPI operation covered).
- `NEXORA_E2E_OPENSEARCH_URL=http://opensearch.nexora.svc:9200 NEXORA_E2E_JAEGER_QUERY_URL=http://jaeger.observability.svc:16686 NEXORA_E2E_BIN_DIR=$PWD/bin go test ./e2e/ -run TestAuthRBACAuditOIDC -count=1` -> `ok github.com/piwi3910/nexora/e2e 15.476s`.
- Not committed (lead commits).

Closing evidence (lead, 2026-09-14):

- TestGUIFleet: N/A — no test with this name exists (criterion was auto-generated from plan text); the behaviour is covered by the task's actual tests, which passed in the full suite (engine/mgmt/web/e2e/bench/deploy) passed in the dev pod during the pre-release hardening sweep at 93dcd04
- TestGUICoverage: passed in the full suite (engine/mgmt/web/e2e/bench/deploy) passed in the dev pod during the pre-release hardening sweep at 93dcd04
- gate/commit: commit gate passed on every commit for this task; todo last committed in cb5b8d3
