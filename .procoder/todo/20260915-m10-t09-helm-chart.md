# M10 T9: Helm chart

Status: open
Created: 2026-09-15

## Description

Implements Task 9 of `.procoder/plans/nexora-m10-querylog-backends.md` (spec `.procoder/specs/nexora-m10-querylog-backends.md`). Done when every step is done, its tests pass in the dev pod and the change is committed.

## Acceptance criteria

- [x] Create `deploy/deploytest/helm_querylog_test.go`:
- [x] Run `scripts/dev-exec.sh 'go test ./deploy/deploytest -run TestHelmQueryLogBackends -count=1'` and
- [x] Implement:
- [x] If the M9 CRD grep matched, add `clickhouse` and `loki` with the same fields to that CRD or type
- [x] Run
- [x] Run `scripts/pc-format.sh deploy/helm/nexora/values.yaml`.
- [ ] Report the paths. Commit message: `helm: clickhouse and loki query-log settings`.

## Evidence

- Red (toolbox-m10): `go test ./deploy/deploytest -run TestHelmQueryLogBackends -count=1` → FAIL,
  `at '/mgmt/querylog/backend': value must be one of 'builtin', 'opensearch'` (the schema rejection).
- Green (toolbox-m10): `go test ./deploy/deploytest -count=1 -skip TestOperationsDocNamesQueryLogSettings -v`
  → `--- PASS: TestHelmQueryLogBackends`, `--- PASS: TestHelmTemplate`, `--- PASS: TestHelmAISecretWiring`.
  The only failures are other wave-2 tasks' in-progress tests: `TestKwClickHouseManifest`,
  `TestKwCollectorFansOutQueryLogs` (Task 10) and `TestOperationsDocNamesQueryLogSettings` (Task 11).
- `helm lint deploy/helm/nexora --strict -f deploy/helm/nexora/ci/lint-values.yaml` → `1 chart(s) linted, 0 chart(s) failed`.
- `go vet ./deploy/deploytest` OK, `gofmt -l deploy/deploytest` empty,
  `scripts/pc-format.sh deploy/helm/nexora/values.yaml deploy/helm/nexora/values.schema.json` exit 0.
- kw render (`-f deploy/kw/values-kw.yaml`) diffed against the HEAD chart: byte-identical, so no golden
  regenerated (`deploy/deploytest/testdata/kw-render.golden.yaml` does not exist in this worktree).
- Mutation: dropping `readOnly: true` from the querylog-password mount → `password mount = ...` FAIL; restored → ok.
- M9 CRD grep `grep -rln 'opensearch' deploy/operator config` → empty (directories absent):
  no M9 query-log backend enum found (grep output empty).
- No credential in values or ConfigMaps: values hold only `passwordSecret {name, key}`; the password is a
  Secret volume item `key → password` at `/etc/nexora/querylog`.
