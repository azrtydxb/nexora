# Helm templates are Go templates, not YAML.
deploy/helm/*/templates/
# Raw helm render golden — must match helm output byte-for-byte.
deploy/deploytest/testdata/kw-render.golden.yaml
deploy/deploytest/testdata/kw-pairs-render.golden.yaml
# controller-gen owns CRD formatting; keep generated outputs reproducible.
deploy/operator/crds/*.yaml
deploy/helm/nexora-operator/crds/*.yaml
