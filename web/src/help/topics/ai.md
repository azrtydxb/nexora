<!-- operations: AI -->

# AI

The AI features suggest; they never change configuration on their own. Setup, privacy, bounds,
metrics and MCP clients are described in the `AI` section of `docs/operations.md`.

## Enabling AI

See the `AI` section of `docs/operations.md`.

## Insights

See the `AI` section of `docs/operations.md`.

## Recommendations

See the `AI` section of `docs/operations.md`.

## Assistant

See the `AI` section of `docs/operations.md`.

## Forecasts

See the `AI` section of `docs/operations.md`.

## Rollout risk

See the `AI` section of `docs/operations.md`.

## Threat checks

See the `AI` section of `docs/operations.md`.

## RPZ suggestions

See the `AI` section of `docs/operations.md`.

## MCP

See the `AI` section of `docs/operations.md`.
