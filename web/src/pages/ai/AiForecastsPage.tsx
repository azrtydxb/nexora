import { AiPage } from "@/components/ai/AiOff";

// M11 Task 27 fills this page.
export function AiForecastsPage() {
  return (
    <AiPage
      title="Forecasts"
      description="Upstream health predictions and capacity forecasts, with when each was generated."
    >
      {null}
    </AiPage>
  );
}
