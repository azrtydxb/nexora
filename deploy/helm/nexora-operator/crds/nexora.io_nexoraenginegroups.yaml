---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: nexoraenginegroups.nexora.io
spec:
  group: nexora.io
  names:
    categories:
    - nexora
    kind: NexoraEngineGroup
    listKind: NexoraEngineGroupList
    plural: nexoraenginegroups
    shortNames:
    - nxeg
    singular: nexoraenginegroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.groupName
      name: Group
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.engineCount
      name: Engines
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NexoraEngineGroup manages an engine group and its join token
          through the management API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              NexoraEngineGroupSpec is a management-plane engine group of a NexoraInstallation. Only fields set here
              are managed; unset fields keep whatever the GUI or API set.
            properties:
              deletionPolicy:
                default: Retain
                enum:
                - Retain
                - Delete
                type: string
              description:
                type: string
              extraACLCIDRs:
                items:
                  type: string
                type: array
              filterIndexMaxBytes:
                format: int64
                type: integer
              groupName:
                description: GroupName defaults to metadata.name.
                pattern: ^[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?$
                type: string
              installationRef:
                description: LocalRef names an object in the same namespace.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              joinToken:
                default: {}
                description: JoinTokenSpec controls the join token the controller
                  keeps in a Secret.
                properties:
                  labels:
                    additionalProperties:
                      type: string
                    maxProperties: 32
                    type: object
                  maxUses:
                    format: int32
                    type: integer
                  renewBefore:
                    default: 720h
                    type: string
                  revokeGracePeriod:
                    default: 10m
                    type: string
                  secretName:
                    description: SecretName defaults to <cr name>-join-token.
                    type: string
                  ttl:
                    default: 8760h
                    type: string
                type: object
                x-kubernetes-validations:
                - message: joinToken.ttl must be between 1m and 8760h
                  rule: '!has(self.ttl) || (duration(self.ttl) >= duration(''1m'')
                    && duration(self.ttl) <= duration(''8760h''))'
                - message: joinToken.renewBefore must be less than joinToken.ttl
                  rule: '!has(self.ttl) || !has(self.renewBefore) || duration(self.renewBefore)
                    < duration(self.ttl)'
              otlpEndpoint:
                type: string
              rollout:
                description: RolloutSpec is the engine group's rollout settings; unset
                  fields keep the management plane's values.
                properties:
                  ackTimeoutSeconds:
                    format: int32
                    type: integer
                  canaryCount:
                    format: int32
                    type: integer
                  canaryPercent:
                    format: int32
                    type: integer
                  healthWindowSeconds:
                    format: int32
                    type: integer
                  maxServfailRatio:
                    description: MaxServfailRatio is a decimal string such as "0.05".
                    type: string
                  minHealthQueries:
                    format: int32
                    type: integer
                  strategy:
                    type: string
                type: object
              upstreamMode:
                enum:
                - inherit
                - override
                type: string
            required:
            - installationRef
            type: object
            x-kubernetes-validations:
            - message: groupName is immutable
              rule: has(self.groupName) == has(oldSelf.groupName) && (!has(self.groupName)
                || self.groupName == oldSelf.groupName)
            - message: installationRef is immutable
              rule: self.installationRef.name == oldSelf.installationRef.name
          status:
            description: NexoraEngineGroupStatus is the observed state of a NexoraEngineGroup.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              engineCount:
                format: int32
                type: integer
              groupID:
                type: string
              joinTokenExpiresAt:
                format: date-time
                type: string
              joinTokenID:
                type: string
              joinTokenSecret:
                type: string
              observedGeneration:
                format: int64
                type: integer
              previousJoinTokenID:
                type: string
              previousJoinTokenRevokeAt:
                format: date-time
                type: string
              revision:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
